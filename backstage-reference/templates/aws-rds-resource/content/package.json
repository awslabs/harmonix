{
  "name": "${{ values.component_id }}",
  "private": true,
  "version": "0.1.0",
  "description": "${{ values.description }}",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "${{ values.owner }}",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.10.0"
  }
}
