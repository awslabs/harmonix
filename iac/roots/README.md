The roots directory should include all top-level CDK projects.

A top-level project is defined as a directory containing a file
that defines a new CDK app that you would want to run "cdk deploy" 
on.
